#include<bits/stdc++.h>
using namespace std;
 
int main(){
	vector<int> a = {1,2,3,4,5,6};
    int n = a.size();
    for(int i = 0;i<n/2;i++){
       swap(a[i],a[n-i-1]);
    }
    for(int i = 0;i<n;i++){ cout<<" "<< a[i];}
	return 0;
}
